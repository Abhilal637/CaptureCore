<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Category</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans text-gray-800">
  <div class="flex min-h-screen">
    <!-- Sidebar -->
    <%- include('../partial/admin/sidebar') %>
    <!-- Main Content -->
    <main class="flex-1 p-6">
      <!-- Header -->
      <div class="flex justify-between items-center mb-6">
        <h1 class="text-2xl font-semibold">Edit Category</h1>
        <a href="/admin/category" class="px-4 py-2 bg-gray-200 rounded hover:bg-gray-300">Back to Categories</a>
      </div>
      <div class="flex justify-center">
        <form method="post" action="/admin/category/edit/<%= category._id %>" class="bg-white rounded-lg shadow-lg p-8 w-full max-w-md flex flex-col gap-4">
          <label class="font-bold text-lg">Category Name</label>
          <input type="text" name="name" value="<%= category.name %>" class="border rounded px-4 py-2 text-lg" required />
          <label class="font-bold text-lg">Description</label>
          <textarea name="description" class="border rounded px-4 py-2 text-lg" required><%= category.description %></textarea>
          <label class="font-bold text-lg">Status</label>
          <select name="active" class="border rounded px-4 py-2 text-lg">
            <option value="true" <%= category.active ? 'selected' : '' %>>Active</option>
            <option value="false" <%= !category.active ? 'selected' : '' %>>Inactive</option>
          </select>
          <button type="submit" class="px-4 py-2 text-lg font-bold bg-blue-600 text-white rounded shadow hover:bg-blue-700 transition-all">Update Category</button>
        </form>
      </div>
    </main>
  </div>
</body>
</html>
