<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .input-focus:focus {
      border-color: #2563eb;
      box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.2);
    }
    .btn-hover:hover {
      transform: translateY(-1px);
      box-shadow: 0 6px 16px rgba(0, 0, 0, 0.2);
    }
    .fade-in {
      animation: fadeIn 0.6s ease-out;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body class="min-h-screen bg-gradient-to-br from-gray-100 via-white to-gray-200 flex flex-col">

  <!-- Navbar -->
  <%- include('../partial/user/navbar') %>

  <!-- Main Container -->
  <div class="flex-grow flex items-center justify-center px-4 py-12">
    <div class="w-full max-w-md">
      
      <!-- Logo / Icon -->
      <div class="text-center mb-8 fade-in">
        <div class="w-16 h-16 mx-auto flex items-center justify-center bg-gradient-to-tr from-gray-900 to-gray-700 rounded-full shadow-lg mb-6">
          <svg class="w-7 h-7 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                  d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 
                     00-7 7h14a7 7 0 00-7-7z" />
          </svg>
        </div>
        <h1 class="text-2xl font-bold text-gray-900">Welcome Back</h1>
        <p class="text-gray-500 text-sm">Sign in to continue to CaptureCore</p>
      </div>

      <!-- Login Card -->
      <div class="bg-white/70 backdrop-blur-lg rounded-2xl shadow-xl border border-gray-100 p-8 fade-in">
        <form method="POST" action="/login" class="space-y-6">

          <!-- Error -->
          <% if (error) { %>
            <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-md text-sm">
              <%= error %>
            </div>
          <% } %>

          <!-- Email -->
          <div>
            <label for="email" class="block text-sm font-medium text-gray-700 mb-2">Email Address</label>
            <input type="text" name="email" id="email"
              value="<%= locals.body && body.email ? body.email : '' %>"
              class="w-full px-3 py-2 border border-gray-300 rounded-lg text-gray-900 placeholder-gray-400 focus:outline-none input-focus transition-all duration-200 <%= locals.errors && errors.email ? 'border-red-300' : '' %>"
              placeholder="Enter your email">
            <% if (locals.errors && errors.email) { %>
              <p class="text-red-500 text-xs mt-1"><%= errors.email.join(', ') %></p>
            <% } %>
          </div>

          <!-- Password -->
          <div>
            <label for="password" class="block text-sm font-medium text-gray-700 mb-2">Password</label>
            <input type="password" name="password" id="password"
              class="w-full px-3 py-2 border border-gray-300 rounded-lg text-gray-900 placeholder-gray-400 focus:outline-none input-focus transition-all duration-200 <%= locals.errors && errors.password ? 'border-red-300' : '' %>"
              placeholder="Enter your password">
            <% if (locals.errors && errors.password) { %>
              <p class="text-red-500 text-xs mt-1"><%= errors.password.join(', ') %></p>
            <% } %>
          </div>

          <!-- Forgot Password -->
          <div class="flex justify-end">
            <a href="/forgot-password" class="text-sm text-blue-600 hover:underline">Forgot your password?</a>
          </div>

          <!-- Login Button -->
          <button type="submit" 
            class="w-full bg-gradient-to-r from-gray-900 to-gray-700 text-white font-medium py-3 px-4 rounded-lg transition-all duration-200 btn-hover">
            Sign In
          </button>

          <!-- Divider -->
          <div class="flex items-center my-6">
            <div class="flex-grow border-t border-gray-300"></div>
            <span class="px-3 text-gray-500 text-sm">or</span>
            <div class="flex-grow border-t border-gray-300"></div>
          </div>

          <!-- Google Login -->
          <a href="/auth/google" 
            class="w-full flex items-center justify-center gap-3 border border-gray-300 text-gray-700 px-4 py-2.5 rounded-lg hover:bg-gray-50 transition-all duration-200 font-medium">
            <img src="https://www.svgrepo.com/show/475656/google-color.svg" alt="Google" class="w-5 h-5">
            Continue with Google
          </a>

          <!-- Sign Up -->
          <p class="text-center text-sm text-gray-600 mt-4">
            Don’t have an account?
            <a href="/signup" class="text-blue-600 font-medium hover:underline">Sign up</a>
          </p>
        </form>
      </div>
    </div>
  </div>
  <!-- Footer -->
  <footer class="bg-white border-t border-gray-200 mt-16">
    <div class="max-w-7xl mx-auto px-6 py-12 grid grid-cols-1 md:grid-cols-4 gap-8">
      
      <!-- Brand -->
      <div>
        <h2 class="font-bold text-lg text-gray-900 mb-3">CaptureCore</h2>
        <p class="text-sm text-gray-500 mb-2">capturecore@info.com</p>
        <p class="text-sm text-gray-500 mb-4">123 Main St, City, Country</p>
        <div class="flex gap-3">
          <a href="#" class="w-9 h-9 flex items-center justify-center bg-gray-100 rounded-full hover:bg-gray-200 transition">
            <svg class="w-4 h-4 text-gray-600" fill="currentColor" viewBox="0 0 24 24">
              <path d="M22 12a10 10 0 1 0-11.5 9.9v-7h-2v-3h2v-2.3c0-2 1.2-3.1 3-3.1.9 0 1.8.1 1.8.1v2h-1c-1 0-1.3.6-1.3 1.2V12h2.3l-.4 3h-1.9v7A10 10 0 0 0 22 12"></path>
            </svg>
          </a>
          <a href="#" class="w-9 h-9 flex items-center justify-center bg-gray-100 rounded-full hover:bg-gray-200 transition">
            <svg class="w-4 h-4 text-gray-600" fill="currentColor" viewBox="0 0 24 24">
              <path d="M23 3a10.9 10.9 0 0 1-3.1 1A5.4 5.4 0 0 0 22.4.4a10.8 10.8 0 0 1-3.5 1.4A5.4 5.4 0 0 0 11.7 6a15.3 15.3 0 0 1-11-5.6 5.4 5.4 0 0 0 1.7 7.2A5.2 5.2 0 0 1 1 6.8v.1a5.4 5.4 0 0 0 4.3 5.3A5.4 5.4 0 0 1 1 12.1v.1A5.4 5.4 0 0 0 5 17a10.8 10.8 0 0 1-6.7 2.3c-.4 0-.8 0-1.2-.1A15.3 15.3 0 0 0 8 21c9.5 0 14.7-7.9 14.7-14.7v-.7A10.5 10.5 0 0 0 23 3z"></path>
            </svg>
          </a>
          <a href="#" class="w-9 h-9 flex items-center justify-center bg-gray-100 rounded-full hover:bg-gray-200 transition">
            <svg class="w-4 h-4 text-gray-600" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 2.2c3.2 0 3.6 0 4.9.1 1.2.1 2 .3 2.5.5a4.6 4.6 0 0 1 1.6 1.1c.5.5.9 1 1.1 1.6.2.5.4 1.3.5 2.5.1 1.3.1 1.7.1 4.9s0 3.6-.1 4.9c-.1 1.2-.3 2-.5 2.5a4.6 4.6 0 0 1-1.1 1.6c-.5.5-1 0.9-1.6 1.1-.5.2-1.3.4-2.5.5-1.3.1-1.7.1-4.9.1s-3.6 0-4.9-.1c-1.2-.1-2-.3-2.5-.5a4.6 4.6 0 0 1-1.6-1.1c-.5-.5-.9-1-1.1-1.6-.2-.5-.4-1.3-.5-2.5-.1-1.3-.1-1.7-.1-4.9s0-3.6.1-4.9c.1-1.2.3-2 .5-2.5a4.6 4.6 0 0 1 1.1-1.6c.5-.5 1-0.9 1.6-1.1.5-.2 1.3-.4 2.5-.5 1.3-.1 1.7-.1 4.9-.1M12 0C8.7 0 8.3 0 7 .1c-1.3.1-2.2.3-3 .6-.9.3-1.6.8-2.3 1.5a6.8 6.8 0 0 0-1.5 2.3c-.3.8-.5 1.7-.6 3C0 8.3 0 8.7 0 12s0 3.7.1 5c.1 1.3.3 2.2.6 3 .3.9.8 1.6 1.5 2.3a6.8 6.8 0 0 0 2.3 1.5c.8.3 1.7.5 3 .6 1.3.1 1.7.1 5 .1s3.7 0 5-.1c1.3-.1 2.2-.3 3-.6.9-.3 1.6-.8 2.3-1.5a6.8 6.8 0 0 0 1.5-2.3c.3-.8.5-1.7.6-3 .1-1.3.1-1.7.1-5s0-3.7-.1-5c-.1-1.3-.3-2.2-.6-3a6.8 6.8 0 0 0-1.5-2.3 6.8 6.8 0 0 0-2.3-1.5c-.8-.3-1.7-.5-3-.6C15.7 0 15.3 0 12 0"></path>
              <circle cx="12" cy="12" r="3.5"></circle>
            </svg>
          </a>
        </div>
      </div>

      <!-- Quick Links -->
      <div>
        <h3 class="font-semibold text-gray-900 mb-3">Quick Links</h3>
        <ul class="space-y-2 text-sm">
          <li><a href="#" class="text-gray-500 hover:text-gray-900">Home</a></li>
          <li><a href="#" class="text-gray-500 hover:text-gray-900">About</a></li>
          <li><a href="#" class="text-gray-500 hover:text-gray-900">Shop</a></li>
          <li><a href="#" class="text-gray-500 hover:text-gray-900">Blog</a></li>
          <li><a href="#" class="text-gray-500 hover:text-gray-900">Contact</a></li>
        </ul>
      </div>

      <!-- Help & Info -->
      <div>
        <h3 class="font-semibold text-gray-900 mb-3">Help & Info</h3>
        <ul class="space-y-2 text-sm">
          <li><a href="#" class="text-gray-500 hover:text-gray-900">Track Your Order</a></li>
          <li><a href="#" class="text-gray-500 hover:text-gray-900">Returns & Policies</a></li>
          <li><a href="#" class="text-gray-500 hover:text-gray-900">Shipping & Delivery</a></li>
          <li><a href="#" class="text-gray-500 hover:text-gray-900">FAQs</a></li>
        </ul>
      </div>

      <!-- Contact -->
      <div>
        <h3 class="font-semibold text-gray-900 mb-3">Contact Us</h3>
        <p class="text-sm text-gray-500">123 Main St, City</p>
        <p class="text-sm text-gray-500">capturecore@info.com</p>
        <p class="text-sm text-gray-500">+123 456 7890</p>
      </div>
    </div>

    <!-- Bottom Bar -->
    <div class="border-t border-gray-100 text-center text-xs text-gray-400 py-4">
      &copy; 2024 CaptureCore. Design by <a href="#" class="hover:text-gray-600">TemplateCreator</a>
    </div>
  </footer>

  <script src="/js/inline-validator.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const form = document.querySelector('form[action="/login"]');
      if (form) {
        window.attachInlineValidation(form);
      }
    });
  </script>

</body>
</html>
