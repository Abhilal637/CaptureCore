<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Profile | CaptureCore</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <link rel="stylesheet" href="/css/user/styles.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white font-sans">
  <%- include('../partial/user/navbar') %>

  <%- include('../partial/user/profilesidebar') %>

  
    <!-- Profile Content -->
    <main class="flex-1">
      <% if (success === 'profile_updated') { %>
        <div class="mb-4 p-4 bg-green-100 border border-green-400 text-green-700 rounded">
          ✅ Profile updated successfully!
        </div>
      <% } %>
      <div class="bg-gray-50 shadow rounded p-10">
        <div class="flex flex-col items-center mb-8">
          <!-- Profile Image -->
          <div class="w-32 h-32 rounded-full overflow-hidden bg-gray-200 mb-4">
            <% if (user?.googleProfilePic) { %>
              <img src="<%= user.googleProfilePic %>" alt="Google Profile" class="w-full h-full object-cover">
            <% } else if (user?.profilePicture) { %>
              <img src="/uploads/<%= user.profilePicture %>" alt="Profile Image" class="w-full h-full object-cover">
            <% } else { %>
              <div class="w-full h-full flex items-center justify-center text-gray-400">
                <svg class="w-20 h-20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M5.121 17.804A9 9 0 1112 21a9 9 0 01-6.879-3.196z" />
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                </svg>
              </div>
            <% } %>
          </div>
          <h2 class="text-2xl font-semibold"><%= user?.name || 'User' %></h2>
          <p class="text-sm text-gray-500"><%= user?.email || '' %></p>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
          <div>
            <p class="text-sm text-gray-500">Name</p>
            <p class="text-lg font-medium"><%= user?.name || 'Not Provided' %></p>
          </div>
          <div>
            <p class="text-sm text-gray-500">Email</p>
            <p class="text-lg font-medium"><%= user?.email || 'Not Provided' %></p>
          </div>
          <div>
            <p class="text-sm text-gray-500">Phone</p>
            <p class="text-lg font-medium"><%= user?.phone || user?.mobile || 'Not Provided' %></p>
          </div>
          <div>
            <p class="text-sm text-gray-500">Member Since</p>
            <p class="text-lg font-medium"><%= user?.createdAt ? new Date(user.createdAt).toLocaleDateString() : 'Not Available' %></p>
          </div>
        </div>
      </div>
    </main>
  </div>


</body>
</html>
